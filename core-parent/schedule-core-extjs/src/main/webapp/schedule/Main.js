//todo 定义系统应用页面

Ext.define('kalix.schedule.Main', {
    extend: 'Ext.container.Container',
    style: {
        background: '#fff url(' + CONFIG.restRoot + '/schedule/resources/images/main.png) 100% 100% no-repeat'
    }
});
